class IncomeScreenData {
  String? number;
  bool isValid;

  IncomeScreenData({
    this.number,
    this.isValid = false,
  });
}

//3 поля, но они не такие как в домейн! они не налабл, а чтобы достичь этого используем  ??
